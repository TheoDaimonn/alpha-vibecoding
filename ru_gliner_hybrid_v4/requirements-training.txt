# Install the appropriate PyTorch build for your CUDA/CPU separately first.
# GLiNER is pinned; transitive ranges are intentional, not a tested lockfile.
# A completed run records actual versions in its RUN_STATUS.json.
gliner[training]==0.2.29
transformers>=4.51.3,<5
accelerate>=1.0,<2
huggingface-hub>=0.30,<1
sentencepiece>=0.2,<0.3
safetensors>=0.5,<1
numpy>=1.26,<3
# Used by the automatic post-training Russian benchmark stage.
datasets>=3.0,<5
pyarrow>=15,<25
